RAG repository — Python file overview and purpose
Author: auto-generated by M365 Copilot
Date: 2026-01-28
Repository: https://github.com/madsll/RAG

This document summarizes the likely purpose and typical usage of each Python
file in the repository, based on the repository structure and conventional
naming patterns in Retrieval-Augmented Generation (RAG) projects for
Search and Rescue (SAR). If any description doesn’t match the exact
implementation, please mark it with a comment and we can refine it.

Top-level scripts
------------------
RAG_main.py
  Role: Primary entry point that wires together the RAG pipeline for SAR use cases.
  Typical responsibilities:
    • Load configuration (scenarios, tool sequences).
    • Initialize embedding/model backends and vector index.
    • Ingest processed documents (e.g., IAMSAR manual extracts) and run retrieval.
    • Call an LLM to generate grounded answers.
  Usage: `python RAG_main.py --config <optional_args>` (arguments may vary).

RAG_openai.py
  Role: OpenAI-specific. The same as above, however using OpenAIs embeddings 

batch_run.py
  Role: Batch execution harness for running many queries/scenarios automatically.
  Typical responsibilities:
    • Iterate over predefined scenario lists or prompts.
    • Collect and persist outputs (e.g., JSON/CSV in Results/).
  Usage: `python batch_run.py` with internal lists or CLI args.


PDF parsing and ingestion
-------------------------
pdf_parse.py
  Role: Baseline PDF parsing and text chunking for knowledge ingestion.
  Typical responsibilities:
    • Extract text from IAMSAR_2022 PDFs.
    • Clean, split into chunks with metadata (source, page).
    • Save chunks to disk for later embedding/indexing.

pdf_parse_openai.py
  Role: Parsing + embedding using OpenAI embeddings.
  Typical responsibilities:
    • Generate embeddings with OpenAI (e.g., text-embedding-3-small/large).
    • Write vectorized chunks to a store (filesystem/FAISS/other).


Evaluation and QA
------------------
ragas_test.py
  Role: Quality evaluation with RAGAS-style metrics.
  Typical responsibilities:
    • Compute faithfulness, answer relevance, context precision/recall.
    • Compare model variants or retrieval settings.
  Usage: `python ragas_test.py` (expects prepared Q/A + contexts).

validation.py
  Role: Rule-based or heuristic validation of generated outputs for SAR.
  Typical responsibilities:
    • Check that answers cite sources/snippets.
    • Validate against scenario constraints (e.g., correct units, steps).
    • Optionally filter/flag responses for manual review.

Supporting data (referenced by Python code)
------------------------------------------
IAMSAR_2022/
  Role: Source manuals/guidance forming the retrieval corpus.
Results/
  Role: Output directory for run logs, predictions, metrics, exports.
iamsar_scenarios.json
  Role: Scenario definitions (questions, context constraints).
iamsar_tool_sequences.json
  Role: Canonical tool/procedure sequences for SAR tasks.
tool_suggestions.json
  Role: Mapping of tasks/prompts to suggested tools or procedures.

Notes
-----
• Environment: Expect `.env` or environment variables for API keys (OPENAI_API_KEY).
• Vector store: If present, code use a local FAISS directory or simple
  serialized arrays in Results/.
• Reproducibility: Batch scripts typically fix seeds and log model/embedding names.

